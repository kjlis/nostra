library(party)

req_term <- 36
req_ratio <- 0.88

#load("prediction_model.rda")

#newdf <- data.frame(term = as.integer(req_term), paid_amount_ratio = req_ratio)
#result <- predict(ctree.model_2, new = newdf)

#as.numeric(as.character(result))

req_term